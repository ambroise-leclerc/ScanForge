@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads REQUIRED)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/ScanForgeTargets.cmake")

# Check if the target exists
if(TARGET ScanForge::scanforge)
    set(ScanForge_FOUND TRUE)
else()
    set(ScanForge_FOUND FALSE)
endif()

check_required_components(ScanForge)
